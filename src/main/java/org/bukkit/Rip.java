package org.bukkit;

import lombok.NoArgsConstructor;

@NoArgsConstructor
public class Rip {

	
	public static void materialTest(Material mat) {
		if(mat == Material   // Uncomment this line and start typing '.coal',
		
		// This will result in memory rapidly increasing until the set maximum of eclipse
		// After that the CPU will go to 100% and stay there
		// With -Xmx8G the IDE survives extremely slow typing. 
		// But will still crash when typing fast
	}
	
}
